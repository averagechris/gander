image: nixos/unstable
arch: x86_64
oauth: pages.sr.ht/PAGES:RW
environment:
  NIX_CONFIG: "experimental-features = nix-command flakes"
sources:
  - https://git.sr.ht/~averagechris/gander
tasks:
  - build: |
      cd gander
      nix build .#release-artifact --out-link result-release-artifact
      mkdir -p ~/artifacts
      cp -p result-release-artifact/* ~/artifacts/
      mkdir -p dist/downloads
      cp -p result-release-artifact/* dist/downloads/
  - publish: |
      cd gander
      nix run .#build-pages -- --include-existing-downloads
      nix run .#publish-pages
artifacts:
  - artifacts/gander-v0.1.0-x86_64-linux.tar.gz
  - artifacts/gander-v0.1.0-x86_64-linux.tar.gz.sha256
